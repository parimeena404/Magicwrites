# Database URL
# For development (SQLite):
DATABASE_URL="file:./dev.db"

# For production (PostgreSQL on Vercel):
# DATABASE_URL="postgresql://user:password@host:5432/database?schema=public"

# Note: On Vercel, you'll need to set up a PostgreSQL database
# Recommended: Use Vercel Postgres or another hosted PostgreSQL service
